---
apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
spec:
  containers:
    - name: nginx-container
      image: nginx:1.21.1
      livenessProbe:
        httpGet:
        path: /
        port: 80 
      ports: 
      containerport: 80
      name: http
      protocol: TCP
    
